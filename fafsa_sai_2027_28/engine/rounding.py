def whole(value: float) -> int:
    # Placeholder centralized rounding function.
    # Exact production implementation must follow the final 2027–28
    # specification's field-by-field rounding direction.
    return int(round(value))
